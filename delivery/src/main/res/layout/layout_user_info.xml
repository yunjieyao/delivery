<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/color_efedf0"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingBottom="15dp"
    android:paddingTop="15dp">

    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_user_head_pic"
        android:scaleType="centerCrop"
        android:src="@drawable/icon_portrait" />

    <TextView
        android:id="@+id/tv_user_name"
        style="@style/Text_333.24"
        android:layout_marginTop="8dp"
        android:drawablePadding="5dp"
        android:drawableRight="@drawable/icon_editor"
        android:text="Yvette 女 Love Life!" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_credit_rating"
            style="@style/Text_333.24"
            android:text="@string/msg_credit_rating" />

        <RatingBar
            style="@style/Base.Widget.AppCompat.RatingBar.Small"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/tv_credit_rating"
            android:isIndicator="true"
            android:numStars="5"
            android:rating="100" />
    </LinearLayout>


</LinearLayout>